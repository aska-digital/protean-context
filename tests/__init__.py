"""Behavioral test suites for the protean-context scaffold.

The suites run as `python3 -m unittest tests.test_<name>` from the repository
root, exactly as build/verify-all.py invokes them. Shared helpers live in
tests/pc_support.py; every suite works on explicit temporary directories and
reads no live state.
"""
